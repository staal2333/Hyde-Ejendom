# ============================================================
# Ejendom AI – Environment Variables
# Copy this to .env.local for local development
# Add these as secrets in Railway for production
# ============================================================

# ─── HubSpot ───────────────────────────────────────────────
# Create a Private App in HubSpot → Settings → Integrations → Private Apps
# Required scopes: crm.objects.companies.read, crm.objects.companies.write,
#   crm.objects.contacts.read, crm.objects.contacts.write
HUBSPOT_ACCESS_TOKEN=pat-xx-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ─── OpenAI ────────────────────────────────────────────────
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini

# ─── Cron Security ─────────────────────────────────────────
# Set this to protect the /api/run-research endpoint
# When set, requests must include: Authorization: Bearer <this-secret>
CRON_SECRET=your-random-secret-string-here

# ─── Optional: Tone of Voice ──────────────────────────────
# Customize the AI's writing style for outreach emails
# TONE_OF_VOICE=Professionel men personlig...

# ─── Optional: Example Emails ─────────────────────────────
# Provide example emails for few-shot learning
# EXAMPLE_EMAILS=Eksempel 1: ...
